@using Microsoft.JSInterop
@inherits FormBase

@inject IJSRuntime JsRuntime
@inject ISnackbar Snackbar

<div class="d-flex @CssClass" style="align-items: center; @Style">
    <MudIcon Icon="@CssIcon" Class="me-3"></MudIcon>
    <MudSelect Label="Year" T="int" Value="@year" ValueChanged="SetYear" AdornmentColor="Color.Primary"
               Required="@Required" Placeholder="@Placeholder" FullWidth="true" Class="me-3">
        @foreach (var item in years)
        {
            <MudSelectItem Value="@item">
                @item
            </MudSelectItem>
        }
    </MudSelect>
    <MudSelect Label="Month" T="int" Value="@month" ValueChanged="SetMonth" AdornmentColor="Color.Primary"
               Required="@Required" Placeholder="@Placeholder" FullWidth="true" Class="me-3">
        @foreach (var item in months)
        {
            <MudSelectItem Value="@item">
                @item
            </MudSelectItem>
        }
    </MudSelect>
    <MudSelect Label="Day" T="int" Value="@day" ValueChanged="SetDay" AdornmentColor="Color.Primary"
               Required="@Required" Placeholder="@Placeholder" FullWidth="true" Class="me-3">
        @foreach (var item in days)
        {
            <MudSelectItem Value="@item">
                @item
            </MudSelectItem>
        }
    </MudSelect>
    <MudTextField T="string" Label="Age" Text="@($"{Value.GetAge()} {Translations.Module.Profile.Years}")"
                  Disabled="true" FullWidth="true"></MudTextField>
</div>
<MudText Typo="Typo.caption" Style="margin-left: calc(1.5rem + 12px)">
    @Translations.Module.Profile.BirthdateTip
</MudText>