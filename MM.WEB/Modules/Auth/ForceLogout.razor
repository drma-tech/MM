@page "/logout"
@inherits ComponentCore<ForceLogout>

<script>
    // document.cookie = 'AppServiceAuthSession=; Path=/; Expires=Thu, 01 Jan 1970 00:00:00 GMT';
    // document.cookie = 'AppServiceAuthSession1=; Path=/; Expires=Thu, 01 Jan 1970 00:00:00 GMT';
    document.cookie = 'StaticWebAppsAuthCookie=; Path=/; Expires=Thu, 01 Jan 1970 00:00:00 GMT';
    // document.cookie = 'StaticWebAppsAuthContextCookie=; Path=/; Expires=Thu, 01 Jan 1970 00:00:00 GMT';
</script>

@code {
    protected override void OnAfterRender(bool firstRender)
    {
        if (firstRender) Navigation.NavigateTo("/.auth/logout", forceLoad: true);
    }
}
