@inherits ComponentCore<AskReviewComponent>

@if (CustomHeader != null)
{
    @CustomHeader
}
else
{
    <MudText Typo="Typo.h6" Color="Color.Info" Align="Align.Center" Class="@Css.Build().Small("my")">
        @GlobalTranslations.WriteReviewSubtitle
    </MudText>
}

@if (Platform != MM.Shared.Enums.Platform.webapp)
{
    <div class="@Css.Build().Raw("store-grid").Small("mb")">
        @if (Platform == SD.Shared.Enums.Platform.windows)
        {
            <MudButton Href="@_windowsReview" OnClick="Reviewed" Variant="Variant.Filled" FullWidth="true" Color="ActionColor" Class="vertical-button">
                <MudImage Src="/logo/microsoft-store.png" Style="max-width: 35px;"></MudImage> Microsoft Store (Store)
            </MudButton>
            <MudButton Href="@_windowsStore" Target="_blank" OnClick="Reviewed" Variant="Variant.Filled" FullWidth="true" Color="ActionColor" Class="vertical-button">
                <MudImage Src="/logo/microsoft-store.png" Style="max-width: 35px;"></MudImage> Microsoft Store (Site)
            </MudButton>
        }
        else if (Platform == SD.Shared.Enums.Platform.play)
        {
            <MudButton Href="@_googleReview" OnClick="Reviewed" Variant="Variant.Filled" FullWidth="true" Color="ActionColor" Class="vertical-button">
                <MudImage Src="/logo/google-play.png" Style="max-width: 35px;"></MudImage> Google Play (Store)
            </MudButton>
            <MudButton Href="@_googleStore" Target="_blank" OnClick="Reviewed" Variant="Variant.Filled" FullWidth="true" Color="ActionColor" Class="vertical-button">
                <MudImage Src="/logo/google-play.png" Style="max-width: 35px;"></MudImage> Google Play (Site)
            </MudButton>
        }
        else if (Platform == SD.Shared.Enums.Platform.ios)
        {

            <MudButton Href="@_appleReview" OnClick="Reviewed" Variant="Variant.Filled" FullWidth="true" Color="ActionColor" Class="vertical-button">
                <MudImage Src="/logo/app-store.png" Style="max-width: 35px;"></MudImage> App Store (Store)
            </MudButton>
            <MudButton Href="@_appleStore" Target="_blank" OnClick="Reviewed" Variant="Variant.Filled" FullWidth="true" Color="ActionColor" Class="vertical-button">
                <MudImage Src="/logo/app-store.png" Style="max-width: 35px;"></MudImage> App Store (Site)
            </MudButton>
        }
        else if (Platform == SD.Shared.Enums.Platform.huawei)
        {

            <MudButton Href="@_huaweiReview" OnClick="Reviewed" Variant="Variant.Filled" FullWidth="true" Color="ActionColor" Class="vertical-button">
                <MudImage Src="/logo/huawei.png" Style="max-width: 35px;"></MudImage> Huawei AppGallery (Store)
            </MudButton>
            <MudButton Href="@_huaweiStore" Target="_blank" OnClick="Reviewed" Variant="Variant.Filled" FullWidth="true" Color="ActionColor" Class="vertical-button">
                <MudImage Src="/logo/huawei.png" Style="max-width: 35px;"></MudImage> Huawei AppGallery (Site)
            </MudButton>
        }
        else if (Platform == SD.Shared.Enums.Platform.xiaomi)
        {

            <MudButton Href="@_xiaomiReview" OnClick="Reviewed" Variant="Variant.Filled" FullWidth="true" Color="ActionColor" Class="vertical-button">
                <MudImage Src="/logo/xiaomi.png" Style="max-width: 35px;"></MudImage> Xiaomi GetApps (Store)
            </MudButton>
            <MudButton Href="@_xiaomiStore" Target="_blank" OnClick="Reviewed" Variant="Variant.Filled" FullWidth="true" Color="ActionColor" Class="vertical-button">
                <MudImage Src="/logo/xiaomi.png" Style="max-width: 35px;"></MudImage> Xiaomi GetApps (Site)
            </MudButton>
        }
    </div>
}
else //webapp
{
    <div class="@Css.Build().Raw("store-grid").Small("mb")">
        <MudButton Href="@_windowsStore" Target="_blank" OnClick="Reviewed" Variant="Variant.Filled" FullWidth="true" Color="ActionColor" Class="vertical-button">
            <MudImage Src="/logo/microsoft-store.png" Style="max-width: 35px;"></MudImage> Microsoft Store
        </MudButton>
        <MudButton Href="@_googleStore" Target="_blank" OnClick="Reviewed" Variant="Variant.Filled" FullWidth="true" Color="ActionColor" Class="vertical-button">
            <MudImage Src="/logo/google-play.png" Style="max-width: 35px;"></MudImage> Google Play
        </MudButton>
        <MudButton Href="@_appleStore" Target="_blank" OnClick="Reviewed" Variant="Variant.Filled" FullWidth="true" Color="ActionColor" Class="vertical-button">
            <MudImage Src="/logo/app-store.png" Style="max-width: 35px;"></MudImage> App Store
        </MudButton>
        <MudButton Href="@_huaweiStore" Target="_blank" OnClick="Reviewed" Variant="Variant.Filled" FullWidth="true" Color="ActionColor" Class="vertical-button">
            <MudImage Src="/logo/huawei.png" Style="max-width: 35px;"></MudImage> Huawei AppGallery
        </MudButton>
        <MudButton Href="@_xiaomiStore" Target="_blank" OnClick="Reviewed" Variant="Variant.Filled" FullWidth="true" Color="ActionColor" Class="vertical-button">
            <MudImage Src="/logo/xiaomi.png" Style="max-width: 35px;"></MudImage> Xiaomi GetApps
        </MudButton>
    </div>
}

@if (ShowOtherApps)
{
    <MudText Typo="Typo.h6" Color="Color.Info" Align="Align.Center" Class="@Css.Build().Small("my")">
        @GlobalTranslations.CheckOurApps
    </MudText>

    <div class="@Css.Build().Raw("store-grid").Small("mb")">
        <MudButton Href="https://www.streamingdiscovery.com" Target="_blank" OnClick="Reviewed" Variant="Variant.Filled" FullWidth="true" Color="ActionColor" Class="vertical-button">
            <MudImage Src="/logo/streamingdiscovery.png" Style="max-width: 35px;"></MudImage> Streaming Discovery
        </MudButton>
        <MudBadge Content="@("Beta")" Overlap="true" Color="Color.Warning">
            <MudButton Href="https://www.my-next-spot.com" Target="_blank" OnClick="Reviewed" Variant="Variant.Filled" FullWidth="true" Color="ActionColor" Class="vertical-button">
                <MudImage Src="/logo/next-spot.png" Style="max-width: 35px;"></MudImage> My Next Spot
            </MudButton>
        </MudBadge>
    </div>
}

@if (ShowOtherApps)
{
    <MudDivider Class="@Css.Build().Medium("my")"></MudDivider>
    <MudButton Disabled="CloseDisabled" Size="Size.Medium" Variant="Variant.Filled" FullWidth="true" Color="Color.Error" StartIcon="@Icons.Material.Filled.Close"
               OnClick="@(() => { MudDialog!.Close(); })" Class="@Css.Build().Small("mb")">
        @Button.Close
        @if (CloseDisabled)
        {
            <MudProgressCircular Value="10 - CloseCountdown" Max="10" Size="Size.Small" Color="Color.Warning" Class="ms-3">
                @CloseCountdown
            </MudProgressCircular>
        }
    </MudButton>
}

<style>
    .store-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(85px, 1fr));
        gap: 8px;
    }
</style>

@code {
    [CascadingParameter] private IMudDialogInstance? MudDialog { get; set; }

    [Parameter] public RenderFragment? CustomHeader { get; set; }
    [Parameter] public bool ShowOtherApps { get; set; } = true;
    [Parameter] public bool ShowClose { get; set; } = true;
    [Parameter] public Color ActionColor { get; set; } = Color.Primary;

    public MM.Shared.Enums.Platform Platform { get; set; }

    private bool CloseDisabled { get; set; } = true;
    private int CloseCountdown { get; set; } = 10;

    private readonly string _windowsReview = "ms-windows-store://review/?ProductId=9n8h01jxft99"; //its working
    private readonly string _googleReview = "market://details?id=com.modern_matchmaker.www.twa"; //its old. better to use In-App Review API.
    private readonly string _appleReview = "itms-apps://itunes.apple.com/app/id6756202435?action=write-review"; //its working
    private readonly string _huaweiReview = "appmarket://details?id=C115460887"; //It may work or it may not.
    private readonly string _xiaomiReview = "mimarket://details?id=com.modern_matchmaker.www.twa"; //It may work or it may not.

    private readonly string _windowsStore = "https://apps.microsoft.com/detail/9n8h01jxft99";
    private readonly string _googleStore = "https://play.google.com/store/apps/details?id=com.modern_matchmaker.www.twa";
    private readonly string _appleStore = "https://apps.apple.com/us/app/modern-matchmaker/id6756202435";
    private readonly string _huaweiStore = "https://appgallery.huawei.com/app/C115460887";
    private readonly string _xiaomiStore = "https://global.app.mi.com/details?id=com.modern_matchmaker.www.twa";

    protected override async Task OnInitializedAsync()
    {
        await base.OnInitializedAsync();

        Platform = await AppStateStatic.GetPlatform(JsRuntime);
    }

    protected override async Task ProcessComponentData()
    {
        await StartCloseCountdown();
    }

    private async Task StartCloseCountdown()
    {
        while (CloseCountdown > 0)
        {
            await Task.Delay(1000);

            CloseCountdown--;
            StateHasChanged();
        }

        CloseDisabled = false;
        StateHasChanged();
    }

    private async Task Reviewed()
    {
        await JsRuntime.Utils().SetStorage("store-reviewed", true);
    }
}
